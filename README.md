```
git clone https://github.com/GamerBoyBK1/Tham.git && sudo mv Tham/* Tham/.* /var/www/pterodactyl 2>/dev/null && sudo rm -rf Tham && sudo chown -R www-data:www-data /var/www/pterodactyl && sudo chmod -R 755 /var/www/pterodactyl
```
